spring:
  datasource:
    url: jdbc:mysql://localhost:3308/factura
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect


server:
  port: 8085  # Cambia 8081 al puerto que desees
